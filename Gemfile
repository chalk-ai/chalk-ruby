# typed: strong

source 'https://rubygems.org'

gemspec

gem 'minitest-ci'

group :development do
  gem 'git-precommit'
  gem 'steep' if RUBY_VERSION >= '2.5'
  gem 'yard'
end

group :test do
  gem 'rspec'
  gem 'simplecov'
end
